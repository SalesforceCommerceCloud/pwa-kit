export default {
    baseStyle: (props) => ({
        image: {
            width: '100%',
            filter: 'grayscale(0%)',
            transition: 'all 0.8s ease'
        }
    }),
    parts: [
        'image'
    ]
}
