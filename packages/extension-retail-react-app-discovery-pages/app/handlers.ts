export const handlers = ({runtime, app}: any) => {
    // NOTE: The base extension has to have this or else the resolution will fail,
    // we might have to think of having defaults in the SDK so no single extensions 
    // is differently structured from the others.
}