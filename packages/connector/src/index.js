import {DemoConnector} from '@mobify/commerce-integrations/dist/connectors/demo-connector'

export default class Connector extends DemoConnector {
    // Define overrides and extensions in this class
}
