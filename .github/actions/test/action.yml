name: test
inputs:
  name:
    description: name
  proj_directory:
    description: proj_directory

description: Test
runs:
  using: composite
  steps:
    - name: Parse input values
      shell: bash
      run: |
        echo "The input variable1 is: ${{ inputs.name }}"
        echo "The input variable2 is: ${{ inputs.proj_directory }}"
